import "./Image.css";

/**Componente Image ⇒ Crea un componente que retorne un <image> y en su interior
 * podéis seleccionar cualquier imagen de ReactJS. */

const Image = () => {
  return <img src="/public/react.png" className="logoReact" alt="React logo" />;
};

export default Image;
