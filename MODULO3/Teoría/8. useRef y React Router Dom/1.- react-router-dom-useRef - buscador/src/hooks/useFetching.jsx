/**
 * Este hook es para manejar el proceso de carga de datos en una aplicación.
 * 
 * Utiliza useState para inicializar un estado local llamado state. 
 * Este estado tiene tres propiedades: data, isLoading, y hasError.
>> data es el lugar donde se almacenarán los datos recuperados después de una solicitud de carga.
>> isLoading es un indicador booleano que indica si la solicitud está actualmente en progreso o no.
>> hasError es donde se almacenaría información sobre cualquier error que ocurra durante la carga de datos,
 si lo hay.
 */
